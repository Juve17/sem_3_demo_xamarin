<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="demo.Menu"
             Title="Menu">
    <ContentPage.Content>
        <StackLayout>           
            
            <Button Text="Home" x:Name="btnHome" Clicked="btnHome_Clicked" BackgroundColor="White" HorizontalOptions="Fill" />
            <Button Text="Listas" x:Name="btnListas" Clicked="btnListas_Clicked"  BackgroundColor="White" HorizontalOptions="Fill" />
            <CollectionView>
                <CollectionView.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Ususarios</x:String>
                        <x:String>Reportes</x:String>
                    </x:Array>
                </CollectionView.ItemsSource>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>